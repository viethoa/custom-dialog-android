<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/app_primary_color</item>
        <item name="colorPrimaryDark">@color/app_primary_color_dark</item>
        <item name="colorAccent">@color/app_primary_color</item>
    </style>

    <!-- Common edit text -->
    <style name="app_edit_text">
        <item name="android:textColor">@color/text_color_dark</item>
        <item name="android:textColorHint">@color/text_color_light</item>
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>
    </style>
    <!-- Primary buttons -->
    <style name="app_primary_button">
        <item name="android:textColor">@color/text_color_bright</item>
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>
        <item name="android:background">@drawable/app_primary_button</item>
    </style>
    <style name="app_borderless_button">
        <item name="android:textColor">@color/app_primary_color</item>
        <item name="android:textAppearance">?android:attr/textAppearanceSmall</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
